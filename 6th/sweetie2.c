// sweetie2.c -- 使用for循环的计数循环
#include <stdio.h>
int main(void)
{
    const int NUMBER = 22;
    int count;                 // 初始化

    for (count =1; count <= NUMBER; count ++)
        printf("Be my vlaentine!\n");
    
    return 0;
}
